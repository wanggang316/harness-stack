Load and execute the hs-define-architecture skill for defining system architecture at the global level.

Read the skill definition at skills/hs-define-architecture/SKILL.md and follow its Process section step by step.

Steps:
1. Load context — read product-spec.md, existing architecture, scan codebase structure
2. Analyze — map domains, layers, dependencies, question structural decisions
3. Write docs/architecture.md — domains, layers, dependency rules, cross-cutting concerns, technology choices
4. Present for human review — do NOT proceed until approved
