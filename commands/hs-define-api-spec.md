Load and execute the hs-define-api-spec skill for defining the API specification.

Read the skill definition at skills/hs-define-api-spec/SKILL.md and follow its Process section step by step.

Steps:
1. Discover — read architecture.md and conventions.md, sample 5-10 existing endpoints for patterns
2. Challenge — resolve inconsistencies in response shapes, error format, status codes, pagination
3. Write docs/api-spec.md — response format, status codes, error codes, pagination, auth, URL conventions, validation, field conventions
4. Present for human review — do NOT proceed until approved
